# Техдолги

# Техдолги

1. Настроить отправку email-сообщений по форме запроса консультации.

# Статус выполнения

- `next.config.mjs` добавлен с заголовками `Cache-Control` для статики, страницы и файлов `sitemap.xml` / `robots.txt`.
- `public/sitemap.xml` добавлен и содержит все актуальные страницы.
- `public/robots.txt` обновлён и теперь указывает на `sitemap.xml`.
- `app/layout.tsx` — metadataBase, canonical, Open Graph, Twitter metadata, JSON-LD `Organization`/`LocalBusiness` реализованы глобально для всех страниц.
- `Home` — основная SEO-разметка выполнена.
- `About` — используется тот же глобальный layout, поэтому metadata/JSON-LD также действуют на этой странице.
- `Favicon_1.svg` подключен в `app/layout.tsx` как иконка сайта.
- В `BlogsSection.tsx` исправлена доступность карточек: добавлен `tabIndex`, обработка `Enter`/`Space` и `aria-pressed`.
- Аудит доступности проведён: формы, фокусные состояния и клавиатурная навигация улучшены.
- Оптимизация client-side JS — выполнена.
